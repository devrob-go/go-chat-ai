{{- if .Values.authService.ingress.enabled -}}
{{- $fullName := include "auth-service.fullname" . -}}
{{- $svcPort := .Values.authService.service.restPort -}}
{{- if and .Values.global.ingressClassName .Values.authService.ingress.className }}
  {{- if not (hasKey .Values.authService.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.authService.ingress.annotations "kubernetes.io/ingress.class" .Values.authService.ingress.className }}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "auth-service.labels" . | nindent 4 }}
  {{- with .Values.authService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.global.ingressClassName .Values.authService.ingress.className }}
  ingressClassName: {{ .Values.authService.ingress.className }}
  {{- end }}
  {{- if .Values.authService.ingress.tls }}
  tls:
    {{- range .Values.authService.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.authService.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
